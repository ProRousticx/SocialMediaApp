import 'package:flutter/material.dart';

class MyWidgets {
  static MyWidgets instance = MyWidgets();

  Widget customCircularProgressIndicator =
      CircularProgressIndicator(color: Colors.blue.shade800, strokeWidth: 1);
}
